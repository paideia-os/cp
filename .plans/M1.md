# cp M1 — per-milestone notes

**Milestone:** M1 — design + skeleton
**Upstream:** `design/tooling/r49-r50-plan.md` §5.6 in paideia-os.

## Issues (per plan doc §5.6)

| # | Title                                                                       | Landed by                                                                                                          |
|---|-----------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------|
| 1 | cp.M1-001 scaffold + caps.decl (src read cap + dst-parent write cap + TXN)  | `caps.decl`, `manifest.pdxproj`, `design/architecture.md`, `STATUS.md`, `README.md`                                |
| 2 | cp.M1-002 argv surface via libpdx-argv                                      | `src/main.pdx`, `src/print.pdx`, `design/argv-surface.md`                                                          |
| 3 | cp.M1-003 first runnable: single-file cp a b in single TXN                  | `src/pdxfs.pdx`, `src/copy.pdx`, `src/dispatch.pdx`, `src/main.pdx` (wire), `design/pdxfs-notes.md`                |

## Substrate assumptions

- paideia-os kernel exposes sys_read (0), sys_write (1), sys_open (2),
  sys_close (3), sys_exit (60) at HEAD (verified against
  `src/kernel/core/syscall/dispatch.pdx` L256-309).
- paideia-os KIND_PDXFS_FILE (0x195) + KIND_PDXFS_TXN (0x196) landed at
  commit 411ad0e.
- KIND_PDXFS_TXN begin/commit **syscalls** are NOT yet exposed. M1
  stubs the two calls; M2 lands the real path.
- paideia-as ≥ v0.33 is the required toolchain floor.
- libpdx-argv M1 (issues #1, #2, #3) closed 2026-08-21 — the argv
  parse dep is met.

## What lands after M1

- M2: recursive `-r`, cap-tail preservation, real TXN, undo record.
- M3: semantic-pipe records, audit journal, cross-subtree elevate.
- M4: test matrix + QEMU smoke.
- M5: dual-signed release + `.pdxdoc` + mirror push.
